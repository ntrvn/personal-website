import React from 'react';
import {withStyles} from '@material-ui/core';

const styles = {
    
}

const Me = (props) => {
    return (
        <div>asd</div>
    );
};

export default withStyles(styles)(Me);